services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: funded-backend
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    env_file:
      - .env.docker
